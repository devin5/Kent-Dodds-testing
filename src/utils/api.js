export const posts = {
  create: (post) => console.log("created:", post),
  delete: (post) => console.log("deleted:", post),
};

export const users = {
  create: (post) => console.log("created:", post),
  delete: (post) => console.log("deleted:", post),
};
